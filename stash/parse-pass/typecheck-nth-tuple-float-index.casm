rule main = {
    assert nth( [ 1, "hallo", true ], 0 ) = 1
    //~^ second argument of nth must be a positive (>0) Integer constant for tuples
    assert nth( [ 1, "hallo", true ], -1 ) = 1
    //~^ second argument of nth must be a positive (>0) Integer constant for tuples
}
init main
