CASM foo

// tests if initializer dependencies are resovled correctly, that is bar is initialized before foo

function foo: -> Integer initially { bar }
function (static) bar : -> Integer initially { 3 }

init initR

rule initR = seq
	assert foo = 3
	program(self) := undef
endseq
