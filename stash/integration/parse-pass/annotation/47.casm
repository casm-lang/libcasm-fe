// error annotation @6
CASM foo

// cycle in initilizer dependencies

function foo: -> Integer initially { bar }

function (static) bar : -> Integer initially { foo }
//~^ initializer dependency cycle detected: bar => bar => foo

init initR

rule initR = skip
