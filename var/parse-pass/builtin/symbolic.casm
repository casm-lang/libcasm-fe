// cmdline "-s"
CASM symbolic

function (symbolic) a: -> Integer
function b: -> Integer initially { 2 }

init main

rule main = {
  assert symbolic( a )
  assert symbolic( b ) = false

  program( self ) := undef
}
