// error annotation @4

rule main = {
  call (@bar)(3,"Hallo")
  //~^ argument 2 of indirectly called rule `bar` must be `Integer` but was `String`
}

rule bar( p1 : Integer, p2 : Integer ) = skip
init main
