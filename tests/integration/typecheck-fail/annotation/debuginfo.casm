// error annotation @6

function a: Integer -> Integer

rule main = {
  debuginfo trace "test = " + a( "hallo" )
  //~^ type of 1 argument of `a` is String but should be Integer

  program( self ) := undef
}
init main
