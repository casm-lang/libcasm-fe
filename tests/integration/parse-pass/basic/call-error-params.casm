// error annotation @4

rule main = seq
  call (@a) ( "hallo", "welt" )
  //~^ argument 1 of indirectly called rule `a` must be `Integer` but was `String`
endseq

rule a( b : Integer, c : String ) = skip

init main
