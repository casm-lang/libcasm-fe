rule main = {
  call (@a) ( 1, "test" )
  //~^ argument 2 of indirectly called rule `a` must be `Integer` but was `String`

  program( self ) := undef
}

rule a( b : Integer, c : Integer ) = {
  skip
}
init main
